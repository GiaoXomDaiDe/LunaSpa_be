import { GetAllBranchesOptions } from '~/models/request/Branches.requests'

class BranchesService {
  async getAllBranches(options: GetAllBranchesOptions) {
    // const { pipeline, _options } = buildBranchesPipeline(options)
  }
}
